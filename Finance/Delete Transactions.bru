meta {
  name: Delete Transactions
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/finance/transactions/{transaction_id}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
