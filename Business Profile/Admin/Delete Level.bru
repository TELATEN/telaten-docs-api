meta {
  name: Delete Level
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/admin/business/levels/{level_id}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Silver Level",
    "required_points": 250
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
