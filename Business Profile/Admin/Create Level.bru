meta {
  name: Create Level
  type: http
  seq: 2
}

post {
  url: {{base_url}}/admin/business/levels
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Bronze Level",
    "required_points": 100,
    "order": 1,
    "icon": "bronze-icon.png"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
