meta {
  name: Delete Achievement
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/gamification/achievements/{achevement_id}
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
